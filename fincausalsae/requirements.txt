# FinCausalSAE — FULL mode requirements
# For the real research pipeline: Llama-3.1-8B + SAELens + a GPU.
# Python 3.10+ required. See SETUP_GUIDE.md for where to get a GPU.

# Core ML
torch>=2.3.0
transformers>=4.43.0
peft>=0.12.0
trl>=0.9.0
bitsandbytes>=0.43.0
accelerate>=0.31.0

# Mechanistic Interpretability
sae-lens>=3.22.0
transformer-lens>=2.3.0

# Financial Data
yfinance>=0.2.40

# Data
datasets>=2.20.0
pandas>=2.2.0
pyarrow>=16.0.0
numpy>=1.26.0
scipy>=1.13.0

# NLP / FinBERT
sentencepiece>=0.2.0

# API (optional — only needed for LLM-based counterfactual generation
# and auto-labeling of causal features; both have non-API fallbacks)
anthropic>=0.34.0

# Experiment Tracking
mlflow>=2.14.0

# Visualization
matplotlib>=3.9.0
seaborn>=0.13.0

# Utilities
tqdm>=4.66.0
requests>=2.32.0
